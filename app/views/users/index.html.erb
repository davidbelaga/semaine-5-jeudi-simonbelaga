<h3>Voici la liste des utilisateurs inscrits :</3>

<% @users.each do |user| %>
<p><%= user.email %>
  <% if current_user && user.email == current_user.email %>
   (vous) <a href="<%= edit_user_path(user.id) %>" class="btn btn-primary"/>Modifier </a> <%= link_to 'Supprimer', user, method: :delete, class: "btn btn-danger" %>
   <% end %>
 </p>
<% end %>


<%= link_to "Nouveau user", new_user_path, class: "btn btn-success" %>
